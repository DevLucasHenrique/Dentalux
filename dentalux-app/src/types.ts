export interface Service {
  color: string;
  icon: string;
  label: string;
  name: string;
}

export interface Quality {
  icon: string;
  name: string;
  description: string;
}

export interface Testimonial {
  rating: number;
  text: string;
  date: string;
}